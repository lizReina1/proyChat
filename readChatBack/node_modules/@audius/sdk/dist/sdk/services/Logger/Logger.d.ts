import type { LoggerService } from './types';
declare const logLevels: readonly ["debug", "info", "warn", "error"];
declare type LogLevel = typeof logLevels[number];
declare type LoggerConfiguration = {
    logLevel?: LogLevel;
    logPrefix?: string;
};
export declare class Logger implements LoggerService {
    private logLevel;
    private logPrefix;
    constructor(config?: LoggerConfiguration);
    createPrefixedLogger(logPrefix: string): Logger;
    debug(...args: any[]): void;
    info(...args: any[]): void;
    warn(...args: any[]): void;
    error(...args: any[]): void;
}
export {};
