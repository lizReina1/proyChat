import { Hedgehog as HedgehogBase } from '@audius/hedgehog';
import type { IdentityService } from '../identity';
import type { LocalStorage } from '../../utils/localStorage';
import type { SetAuthFn, SetUserFn, CreateKey } from '@audius/hedgehog';
export declare type HedgehogConfig = {
    identityService: IdentityService;
    useLocalStorage?: boolean;
    localStorage?: LocalStorage;
    createKey?: CreateKey;
};
export declare class Hedgehog {
    identityService: IdentityService;
    getFn: IdentityService['getFn'];
    setAuthFn: SetAuthFn;
    setUserFn: SetUserFn;
    instance: HedgehogBase;
    constructor({ identityService, useLocalStorage, localStorage, createKey }: HedgehogConfig);
}
