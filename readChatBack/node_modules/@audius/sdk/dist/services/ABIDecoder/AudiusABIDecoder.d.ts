import abiDecoder from 'abi-decoder';
import type { Log } from 'web3-core';
export declare class AudiusABIDecoder {
    static decodeMethod(contractName: string, encodedABI: string): abiDecoder.DecodedMethod;
    static decodeLogs(_: string, logs: Log[]): abiDecoder.DecodedLog;
    static decodeAbi(contractName: string, encodedABI: string): Map<string, string>;
    static recoverSigner({ encodedAbi, chainId, entityManagerAddress }: {
        encodedAbi: string;
        chainId: string;
        entityManagerAddress: string;
    }): string;
}
