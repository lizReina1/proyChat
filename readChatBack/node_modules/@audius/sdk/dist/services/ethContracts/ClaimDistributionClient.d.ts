import type BN from 'bn.js';
import { ContractClient } from '../contracts/ContractClient';
export declare class ClaimDistributionClient extends ContractClient {
    /**
     * Calls the contract method to check if the claim index has been claimed
     */
    isClaimed(index: number): Promise<any>;
    /**
     * Proxies the calls the contract method to make a claim
     * @param index
     * @param account
     * @param amount
     * @param merkleProof
     * @returns transaction
     */
    claim(index: number, account: string, amount: BN, merkleProof: string[]): Promise<import("../../utils").Maybe<{
        txHash: string;
        txParams: {
            data: string;
            gasLimit: string;
            gasPrice: number;
            nonce: string;
            to: string;
            value: string;
        };
    }>>;
}
